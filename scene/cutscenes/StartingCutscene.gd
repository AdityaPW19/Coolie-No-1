#extends AnimationPlayer
#
#
## Called when the node enters the scene tree for the first time.
#func _ready() -> void:
	#var color_rect = $"../FadeOverlay"
	#play("Starting")
	#color_rect.visible = true
